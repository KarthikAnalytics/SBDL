The aim of this project is to create a sample, configurable, and testable project structure that can be reused for more complex projects.

The project assumes that data is available in Hive and has already been processed. However, Hive itself is not utilized; instead, sample data is used.
